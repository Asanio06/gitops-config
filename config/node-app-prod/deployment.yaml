apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app-prod
spec:
  selector:
    matchLabels:
      app: node-app-prod
  replicas: 2
  template:
    metadata:
      labels:
        app: node-app-prod
    spec:
      containers:
        - name: node-app
          image: asanio06/examplenode-dev:4
          ports:
            - containerPort: 3000
