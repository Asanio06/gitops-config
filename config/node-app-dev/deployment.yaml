apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app-dev
spec:
  selector:
    matchLabels:
      app: node-app-dev
  replicas: 2
  template:
    metadata:
      labels:
        app: node-app-dev
    spec:
      containers:
        - name: node-app
          image: asanio06/examplenode-dev:9
          ports:
            - containerPort: 3000
